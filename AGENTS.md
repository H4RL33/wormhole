# Wormhole Agent Bootstrap

Read `agents/README.md` first. It is the canonical project context.

Before work, inspect `.agents/` and read each applicable `.agents/skills/*/SKILL.md`.

For implementation, read `docs/implementation-rules.md` and follow its dispatch
heuristic and guardrails.

# Build artifacts
/dist/
# Bare `go build ./cmd/x` drops binaries in the repo root; ignore each in
# case anyone skips `make build`.
/wormhole
/gatewayd
/fabric
*.exe
*.test
*.out

# Local env
.env
.env.local

# Agent tooling cache (plugin/skill source, not project content)
/.agents/

# Git worktrees
.worktrees/

# OS/editor
.DS_Store
*.swp
